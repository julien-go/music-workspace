spring:
  datasource:
    url: jdbc:postgresql://localhost:${POSTGRES_PORT:5433}/${POSTGRES_DB:music_workspace}
    username: ${POSTGRES_USER:postgres}
    password: ${POSTGRES_PASSWORD:postgres}
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    enabled: true

jwt:
  # Dev-only signing key, not used in production (see application-prod.yml)
  secret: ${JWT_SECRET:8lWx1XGsfQp6tkHDUAl9/CEUOYkXhvcFDEAfUF2Uvv8=}

logging:
  level:
    org.hibernate.SQL: debug
